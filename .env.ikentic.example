# OpenClaw + Ikentic overlay example .env

# Docker image
OPENCLAW_IMAGE=openclaw:local

# Ports (pick free ports; example uses 19002/19003)
OPENCLAW_GATEWAY_PORT=19002
OPENCLAW_BRIDGE_PORT=19003
OPENCLAW_GATEWAY_BIND=lan

# Gateway token (local dev)
OPENCLAW_GATEWAY_TOKEN=devtoken

# Ikentic plugin mount (host path)
IKENTIC_PLUGIN_DIR=/path/to/ike-agents/.worktrees/feat-ikentic-openclaw-personas

# Ikentic plugin path inside container (must contain openclaw.plugin.json)
IKENTIC_PLUGIN_PATH=/home/node/.openclaw-plugins/ikentic/packages/openclaw-ikentic-plugin

# Ikentic API base URL (QA or prod)
IKENTIC_API_BASE_URL=https://locus-ferb.qa.locusserver.com
# Optional legacy fallbacks (if IKENTIC_API_BASE_URL is unset):
# IKE_API_BASE_URL=https://locus-ferb.qa.locusserver.com
# LOCUS_BASE_URL=https://locus-ferb.qa.locusserver.com
# LOCUS_API_URL=https://locus-ferb.qa.locusserver.com

# Ikentic OAuth (example placeholders)
IKE_OAUTH_ISSUER=http://host.docker.internal:8787
IKE_OAUTH_CLIENT_NAME=OpenClaw Ikentic Plugin
IKE_OAUTH_SCOPE=openid profile email offline_access
# Choose one redirect strategy:
# 1) Explicit redirect URI:
IKE_OAUTH_REDIRECT_URI=http://127.0.0.1:19002/oauth/callback
# 2) Or set host/port (auto-builds redirect URI):
# IKE_OAUTH_REDIRECT_HOST=127.0.0.1
# IKE_OAUTH_REDIRECT_PORT=19002
# Public issuer for browser/callback URLs (host-reachable, usually localhost)
IKE_OAUTH_PUBLIC_ISSUER=http://localhost:8787

# Temp host state (first pass)
OPENCLAW_CONFIG_DIR=~/openclaw-dev/state
OPENCLAW_WORKSPACE_DIR=~/openclaw-dev/state/workspace
# For container-only state, switch to:
# OPENCLAW_CONFIG_DIR=openclaw-state
# OPENCLAW_WORKSPACE_DIR=openclaw-workspace
